---
title: {{it.name}}
description: |
  {{it.docs | summarize}}
---
<span class="dart-code"><strong>{{it.returnType | link}} {{it.name}}
{%- if it.typeParams -%}
  \<{{it.typeParams | join(', ')}}>
{%- endif -%}</strong>({{it.params | params}})
{%- if it.modifier %} {{it.modifier}}{% endif %};</span>

{{it.docs}}
### Implementation
```dart
{{it | displaySource}}
```

{{generateReferences()}}
